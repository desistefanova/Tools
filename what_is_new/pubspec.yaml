name: what_is_new
description: A new Flutter project.
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=2.19.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  http: ^0.13.5
  realm: ^1.0.2
  markdown: ^7.0.1
  provider: ^6.0.5
  pluto_grid: ^6.0.3
  pluto_grid_export: ^1.0.4
  file_picker: ^5.2.5
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true  
  assets:
    - assets/source_config.json
    - assets/atlas_app/realm_config.json
